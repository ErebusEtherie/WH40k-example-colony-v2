"""Pytest configuration and shared fixtures."""

# No special configuration needed - rate limiter automatically detects test environment
# via PYTEST_CURRENT_TEST environment variable which pytest sets automatically.